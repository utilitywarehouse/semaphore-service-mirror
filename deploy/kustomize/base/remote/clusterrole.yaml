kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: semaphore-service-mirror
rules:
- apiGroups: [""]
  resources:
  - services
  - endpoints
  verbs: ["get", "list", "watch"]
---
kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: semaphore-service-mirror-binding
subjects:
- kind: ServiceAccount
  name: semaphore-service-mirror
  namespace: sys-semaphore
roleRef:
  kind: ClusterRole
  name: semaphore-service-mirror
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: semaphore-service-mirror
  namespace: sys-semaphore

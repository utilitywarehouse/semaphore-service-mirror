apiVersion: apps/v1
kind: Deployment
metadata:
  name: semaphore-service-mirror
  labels:
    app: semaphore-service-mirror
spec:
  replicas: 1
  selector:
    matchLabels:
      app: semaphore-service-mirror
  template:
    metadata:
      labels:
        app: semaphore-service-mirror
    spec:
      serviceAccountName: semaphore-service-mirror
      containers:
        - name: semaphore-service-mirror
          image: semaphore-service-mirror
          command:
            - /semaphore-service-mirror
            - -log-level=info
            - -target-kube-config=/etc/config/target-kubeconfig.yaml
          ports:
          - name: readiness-port
            containerPort: 8080
          readinessProbe:
            httpGet:
              path: /healthz
              port: readiness-port
            periodSeconds: 10
            failureThreshold: 1
          volumeMounts:
            - name: config
              mountPath: /etc/config
      volumes:
        - name: config
          secret:
            secretName: semaphore-service-mirror-config
